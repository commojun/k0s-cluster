apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-pvc-nfs
  namespace: default
spec:
  storageClassName: nfs-test
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
