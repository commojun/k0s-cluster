apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-pvc-local
  namespace: default
spec:
  storageClassName: storage-test
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
